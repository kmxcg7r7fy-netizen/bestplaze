# Claude — Entrée pour Claude Code

Bienvenue sur **Best Plaze**.

Avant toute chose :

1. Lis `PROJECT_STATUS.md` à la racine — état actuel, modifs en cours, demandes client.
2. Lis `AGENTS.md` à la racine — conventions de code, stack, structure, pièges à éviter.

Ces deux fichiers contiennent tout le contexte nécessaire. À chaque tâche que tu termines,
pense à mettre à jour `PROJECT_STATUS.md` (au moins la section 5 "Demandes client").

⚠️ **Next.js 16.2.1** : version récente, ne te fie pas à ta mémoire des API.
En cas de doute, regarde `node_modules/next/dist/docs/`.

— Bon code.
